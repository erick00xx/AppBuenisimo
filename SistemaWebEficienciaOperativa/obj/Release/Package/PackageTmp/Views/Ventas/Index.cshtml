@model List<SistemaWebEficienciaOperativa.Models.tbPedidos>
@{
    ViewBag.Title = "Index";
}


<h2 class="text-start">
    <a href="~/Pedidos/Index/"
       class="btn btn-secondary pull-rigth">Volver a Pedidos</a>
</h2>

@*@using (Html.BeginForm("Index", "Helado", FormMethod.Post))
    {
        <div class="text-end">
            @Html.Label("Buscar: ")
            @Html.TextBox("Criterio")
            <button type="submit" class="btn btn-info">Buscar</button>
        </div>
    }*@

<table id="Horizontal" class="table">
    <thead>
    <th style="width:50px;">#</th>
    <th style="width:100px;">Mesa</th>
    <th style="width:100px;">FechaVenta</th>
    <th style="width:50px;">Empleado</th>
    <th style="width:50px;">Sucursal</th>
    <th style="width:250px;">Total</th>
    @*<th style="width:250px;">Acciones</th>*@
    </thead>
    <tbody>
        @foreach (var m in Model)
        {
            <tr>
                <td>@(m.idPedido)</td>
                <td>@(m.tbMesas.codMesa)</td>
                <td>@(m.fechaPedido)</td>
                <td>@(m.tbUsuarios.nombre + " " + m.tbUsuarios.apellido)</td>
                <td>@(m.tbSucursales.nombre)</td>
                <td>S/. @(m.total)</td>
                @*<td style="align-content:center;">
                    <a href="~/Ventas/Ver/@m.idPedido" class="btn btn-warning">
                        Detalles
                    </a>
                    <a href="~/Ventas/AgregarEditar/@m.idPedido" class="btn btn-primary">
                        Modificar
                    </a>
                    <a href="~/Ventas/Eliminar/@m.idPedido" class="btn btn-danger"
                       onclick="return confirm('¿Esta seguro de eliminar el registro...?');">
                        Eliminar
                    </a>
                </td>*@
            </tr>
        }
    </tbody>
</table>

